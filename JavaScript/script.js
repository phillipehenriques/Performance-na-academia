function createTable() {
    var tabela = document.getElementsById('tabela')[0];
    var tbl = document.createElement('table'); 

    // tbl.setAttribute('class','' ) < colocar a classe da div > 

    for (var row = 0; row <= 10; row++) {
        var tr = document.createElement('tr');
        for (var column = 0; column <= 1; column++) {
            }
        }
}

createTable()

function addResult() {
    var resultados = document.getElementById('result')[0]
    var coluna = document.createElement('row')

    for (var linha = 0; linha < 11; linha++) {
        var criarLinha = document.createElement('tr')
    }
}

    addResult()

function addPage() {
    
}